import React from "react";
import {FC} from "react";
import {Nav} from "react-bootstrap";

const Menu: FC<{}> = props => {
    return (
        //TODO: DO MENU
        <Nav>
        </Nav>
    );
}
export default Menu;
