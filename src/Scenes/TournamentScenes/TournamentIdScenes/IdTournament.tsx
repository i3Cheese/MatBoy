import React, {FC} from 'react';
import {Tournament} from "../../../types/models";

const TournamentId: FC<{tour: Tournament}> = (tour) => {
    return (
      <></>
    );
}

export default TournamentId;
